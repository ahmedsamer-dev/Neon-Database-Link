The project is already built (e-commerce system with admin dashboard).
I ONLY need improvements and feature enhancements (DO NOT rebuild the system).

Language:
All UI must be Arabic (RTL layout).

-------------------------------------

Tasks (Enhancements Only):

1. Product Image Upload Enhancement
- Add a second option for product image:
  a) Upload from device (file upload)
  b) Input image via URL (link)
- Admin can choose either option easily in the form.

-------------------------------------

2. Product Stock Status
- If product stock = 0:
  - Product MUST still appear in shop page
  - Display clear label: "نفذت الكمية"
  - Disable "Add to Cart" button

-------------------------------------

3. Admin Product Management Improvements
- Ensure admin can:
  - Add/Edit product name, description, price
  - Upload image (file or URL)
  - Add sizes (simple flexible input like S, M, L)
  - Add colors as text input (e.g. red, blue, #FF0000)

- On Shop Page:
  - Colors must render visually as color circles
  - Sizes must appear selectable and clean UI

-------------------------------------

4. UI / UX Enhancements (Buttons & Interaction)
- Improve all buttons styling:
  - Add smooth hover effect
  - Add transition (0.2–0.3s)
  - Slight scale or color change on hover
  - Modern clean design

-------------------------------------

5. Dashboard Order Filtering System
- In dashboard:
  Add clickable filters (NOT new pages):

  - Total Orders → shows all orders
  - Pending → shows pending orders
  - Processing (قيد التنفيذ) → orders in progress
  - Completed (تم التنفيذ) → confirmed orders

- Add search input to filter orders
- Filters should update results dynamically

-------------------------------------

6. About Page Enhancement (Marketing Style)
- Improve About page content:
  - Written fully in Arabic
  - Add strong marketing tone
  - Explain:
    - من نحن
    - ماذا نقدم
    - لماذا نحن الأفضل
  - Clean modern layout

-------------------------------------

Constraints:
- Do NOT break existing structure
- Do NOT rebuild project
- Only enhance current features
- Keep code clean and modular

-------------------------------------

Output:
- Updated code for each feature
- Clear explanation of what was changed
- Maintain consistency with current project structure